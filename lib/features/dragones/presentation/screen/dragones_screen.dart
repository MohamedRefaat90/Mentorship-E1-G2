import 'package:flutter/material.dart';

class DragonesScreen extends StatelessWidget {
  const DragonesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
